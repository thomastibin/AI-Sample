[project]
name = "samplemcp"
version = "0.1.0"
description = "Minimal MCP SSE server and client"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=0.1.0",
    "uvicorn>=0.30.0",
]

[tool.uv]
package = true

